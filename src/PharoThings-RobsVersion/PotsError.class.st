"
I am signalled when an error occurs. Note that errors in Pots will mostly be internal errors or caused by  the user when configuring items

"
Class {
	#name : #PotsError,
	#superclass : #Error,
	#category : #'PharoThings-RobsVersion'
}
